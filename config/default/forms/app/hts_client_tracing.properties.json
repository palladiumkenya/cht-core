{
        "icon": "icon-mother-child",
        "title": "HTS client tracing form",
        "context": {
                "person": false,
                "place": true,
                "expression": "!contact.muted && contact.contact_type == 'clinic' && (summary.hts_retest_latest.final_result && summary.hts_retest_latest.final_result === '_703_positive_99DCT' && !summary.has_hts_linkage)"
        }
}
