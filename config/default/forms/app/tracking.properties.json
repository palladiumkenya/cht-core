{
  "icon": "icon-follow-up",
  "title": "KP LTFU Tracking",
  "context": {
    "person": false,
    "place": true,
    "expression": "!contact.muted && contact.contact_type == 'universal_client' && (user.role === 'kp_peer_educator' || user.role === 'clinician') && (!(summary.screenedToday && summary.lastHtsService !== ''))"
  }
}