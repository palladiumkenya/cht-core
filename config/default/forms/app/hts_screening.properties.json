{
  "icon": "icon-mother-child",
  "title": "Start HTS encounter",
  "context": {
    "person": false,
    "place": true,
    "expression": "(user.role === 'hts_provider' || user.role === 'clinician') && contact.contact_type === 'client' && !summary.screenedToday && contact.record_purpose !== 'linkage' && (summary.hts_retest_latest.final_result != '_703_positive_99DCT')"
  }
}
