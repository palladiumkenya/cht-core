{
        "icon": "icon-healthcare",
        "title": "PrEP Treatment Verification",
        "context": {
                "person": false,
                "place": true,
                "expression": "!contact.muted && contact.contact_type == 'universal_client' && (user.role === 'kp_case_manager' || user.role === 'clinician') && (!(summary.screenedToday && summary.lastHtsService !== ''))"
        }
}