{
  "icon": "icon-treatment",
  "title": "HTS Linkage Form",
  "context": {
    "person": false,
    "place": true,
    "expression": "(user.role === 'hts_provider' || user.role === 'clinician') && !contact.muted && contact.contact_type === 'universal_client' && summary.hts_retest_latest.final_result == '_703_positive_99DCT' && !summary.has_hts_linkage"
  }
}