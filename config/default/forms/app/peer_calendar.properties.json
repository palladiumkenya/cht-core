{
  "icon": "icon-calendar",
  "title": "Peer Calendar",
  "context": {
    "person": false,
    "place": true,
    "expression": "!contact.muted && contact.contact_type == 'universal_client' && (user.role === 'kp_peer_educator') && (!(summary.screenedToday && summary.lastHtsService !== ''))"
  }
}