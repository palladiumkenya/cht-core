{
        "icon": "icon-mother-child",
        "title": "HTS Initial Form",
        "context": {
                "person": false,
                "place": true,
                "expression": "(user.role === 'hts_provider' || user.role === 'clinician') && !contact.muted && contact.contact_type == 'clinic' && contact.record_purpose !== 'linkage' && (summary.screenedToday && summary.lastHtsService === 'newOrRetest') && (!summary.has_hts_initial || (summary.hts_latest_form.form_type === 'hts_initial_form' && summary.hts_latest_consent.consent === '_1066_no_99DCT'))"
        }
}
